def no_repeat(y)
  year = y.to_s
  i = 0
  while i < year.length
    if year.count(year[i]) > 1
      return false
    end
  i += 1
end
return true
end

def no_repeats(year_start, year_end)
  year_range = (year_start..year_end).to_a
  p year_range
  arr = []
 i = 0
 while i < year_range.length
  if no_repeat(year_range[i]) == true
    arr << year_range[i]
  end
  i += 1
end
return arr
end
